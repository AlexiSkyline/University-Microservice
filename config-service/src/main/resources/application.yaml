server:
  port: 8080

spring:
  cloud:
    config:
      server:
        git:
          default-label: main
          uri: https://github.com/AlexiSkyline/University-Microservice
          search-paths: config-data
  application:
    name: config-service