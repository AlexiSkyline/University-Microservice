server:
  port: 8082

spring:
  application:
    name: grades-service

  datasource:
    url: jdbc:mysql://localhost:3306/university
    username: root
    password: root

  jpa:
    hibernate.ddl-auto: update
    generate-ddl: true
    show-sql: true
    open-in-view: true